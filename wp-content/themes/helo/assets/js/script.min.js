! function(e) {
    "use strict";
    var t = {
        init: function(n) {
            t.config = {
                responsive_menu_width: 1199,
                header: e(".default-blog-header")
            }, e.extend(t.config, n), t.setup()
        },
        setup: function() {
            document.getElementById("helo_offcanvas") && t.offcanvas()
        },
        offcanvas: function() {
            let e = document.getElementById("helo_offcanvas"),
                t = document.getElementById("helo_open_offcanvas"),
                n = document.getElementById("helo_close_offcanvas");
            t.addEventListener("click", () => {
                e.classList.add("show")
            }), n.addEventListener("click", () => {
                e.classList.remove("show")
            }), document.addEventListener("click", n => {
                let i = e.contains(n.target) || t.contains(n.target);
                i || e.classList.remove("show")
            });
            let i = document.querySelectorAll(".helo-mb-menu-items li > a");
            i.forEach(e => {
                let t = e.nextElementSibling;
                t && "UL" === t.tagName && t.classList.contains("dp-menu") && (e.querySelector(".nav-direction-icon").setAttribute("data-icon", "+"), e.querySelector(".nav-direction-icon").addEventListener("click", function(e) {
                    let t = this.parentElement.nextElementSibling;
                    if (t && "UL" === t.tagName && t.classList.contains("dp-menu")) {
                        e.preventDefault();
                        let n = "true" === t.getAttribute("aria-expanded");
                        t.setAttribute("aria-expanded", !n), t.style.display = n ? "none" : "block", t.style.opacity = n ? 0 : 1, n ? this.setAttribute("data-icon", "+") : this.setAttribute("data-icon", "-")
                    }
                }))
            })
        }
    };
    e(document).ready(t.init)
}(jQuery);